services:
  - type: web
    name: cors-proxy-ollama
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node cors-proxy.js
    envVars:
      - key: NODE_ENV
        value: production
